# PDEF 实现状态对比

本文档对比**设计文档**与**当前实现**，标识已完成和待实现的功能。

---

##  已完成的核心功能

### 1. 基础语法 (100%)

| 功能 | 状态 | 说明 |
|------|------|------|
| 不用struct关键字定义结构 |  | 直接用结构名 `Header { ... }` |
| @protocol元信息 |  | name, ports, endian |
| @const常量定义 |  | 支持常量引用 |
| @filter过滤规则 |  | 通过赋值语法定义条件 |
| 嵌套结构 |  | 支持多层嵌套，自动展开 |
| 部分匹配 |  | packet_len >= min_size 即可 |

### 2. 数据类型 (85%)

| 类型 | 状态 | 说明 |
|------|------|------|
| uint8, uint16, uint32, uint64 |  | 完全支持 |
| int8, int16, int32, int64 |  | 完全支持 |
| bytes[N] |  | 固定长度字节数组 |
| string[N] |  | 等同于bytes[N] |
| varbytes |  | 已定义类型，但**未强制校验必须在末尾** |
| float |  | **未实现** |
| double |  | **未实现** |

### 3. 比较操作符 (70%)

| 操作符 | 状态 | 说明 |
|--------|------|------|
| = / == |  | 等于 |
| != |  | 不等于 |
| > |  | 大于 |
| >= |  | 大于等于 |
| < |  | 小于 |
| <= |  | 小于等于 |
| & mask = value |  | 位掩码匹配 |
| in [v1, v2, v3] |  | **未实现** |
| !in [v1, v2] |  | **未实现** |
| =~ "regex" |  | **未实现** |
| !~ "regex" |  | **未实现** |
| in [100..200] |  | **未实现**（范围语法糖） |

### 4. 嵌套和访问 (90%)

| 功能 | 状态 | 说明 |
|------|------|------|
| 单层嵌套 |  | `A { B b; }` |
| 多层嵌套 |  | `A { B b; }`, `B { C c; }` |
| 多对象嵌套 |  | `A { B b; C c; D d; }` |
| 点号访问 |  | `b.field`, `b.c.field` |
| 对象数组 |  | `Item items[10]` **未实现** |
| 数组索引访问 |  | `items[0].field` **未实现** |

### 5. 字节码编译 (100%)

| 功能 | 状态 | 说明 |
|------|------|------|
| 配置加载时编译 |  | DSL → 字节码 |
| LOAD指令 |  | 所有整数类型，带字节序 |
| CMP指令 |  | EQ, NE, GT, GE, LT, LE, MASK |
| 控制流指令 |  | JUMP_IF_FALSE, RETURN_TRUE/FALSE |
| 字段offset预计算 |  | 编译时展开嵌套并计算绝对offset |
| min_packet_len优化 |  | 快速长度检查 |

### 6. 运行时执行 (100%)

| 功能 | 状态 | 说明 |
|------|------|------|
| 零拷贝执行 |  | 直接访问报文offset |
| 边界检查 |  | 每次访问前检查长度 |
| 字节序转换 |  | 内联函数，编译器优化 |
| 短路求值 |  | JUMP_IF_FALSE提前退出 |

---

##  未实现的功能

### 1. 高优先级（影响实用性）

#### 1.1 集合操作符 `in` / `!in`

**设计文档要求**：
```pdef
filter Header check {
    packet_type in [1, 2, 3, 10, 20];
    flags !in [0xFF, 0xFE];
}
```

**当前实现**：
-  词法分析器没有 `TOKEN_IN`
-  解析器没有 `COND_IN`
-  字节码没有 `OP_CMP_IN`

**需要改动**：
1. `lexer.c`: 添加 `in` 关键字识别
2. `parser.c`:
   - 添加 `COND_IN` / `COND_NOT_IN`
   - 解析 `[v1, v2, v3]` 语法
   - 存储值列表
3. `executor.c`: 添加 `OP_CMP_IN` 执行逻辑

---

#### 1.2 变长字段位置校验

**设计文档要求**：
```pdef
//  正确
Packet {
    uint32 id;
    byte[] payload;  // 变长必须最后
}

//  错误：应该报错
BadPacket {
    uint32 id;
    byte[] data;     //  变长在中间
    uint32 checksum;
}
```

**当前实现**：
-  已定义 `FIELD_TYPE_VARBYTES`
-  **未在解析器中强制校验**

**需要改动**：
```c
// 在 parser.c 的 parse_struct_def() 中添加
static bool validate_variable_fields(const StructDef* s) {
    bool found_variable = false;
    for (uint32_t i = 0; i < s->field_count; i++) {
        if (s->fields[i].is_variable) {
            found_variable = true;
        } else if (found_variable) {
            // 变长字段后面还有定长字段，错误！
            return false;
        }
    }
    return true;
}
```

---

#### 1.3 对象数组 `Type array[N]`

**设计文档要求**：
```pdef
Item {
    uint32 item_id;
    uint16 count;
}

Inventory {
    uint32 player_id;
    Item items[10];        // 对象数组
}

filter Inventory check {
    items[0].item_id > 0;  // 访问第一个元素
    items[5].count > 100;
}
```

**当前实现**：
-  解析器不支持 `Type field[N]` 语法
-  没有数组索引访问 `field[0]`

**需要改动**：
1. 解析器：识别 `Type name[size]` 语法
2. 字段展开：将 `items[10]` 展开为 `items[0], items[1], ..., items[9]`
3. 过滤器：支持 `items[0].field` 访问

---

### 2. 中优先级（增强表达能力）

#### 2.1 正则匹配 `=~` / `!~`

**设计文档要求**：
```pdef
filter HttpRequest api {
    method in ["POST", "PUT"];
    uri =~ "^/api/.*";     // 正则匹配
}
```

**当前实现**：
-  词法分析器没有 `=~` token
-  运行时没有正则引擎

**需要改动**：
1. 引入正则库（如 PCRE 或自己实现简化版）
2. 编译时预编译正则表达式
3. 运行时执行匹配（性能较慢）

**性能影响**：
- 正则匹配比整数比较慢 **100-1000倍**
- 建议只用于低频场景（HTTP等文本协议）

---

#### 2.2 范围语法糖 `in [min..max]`

**设计文档要求**：
```pdef
filter Header range {
    player_id in [1000..2000];  // 语法糖
}

// 等价于
filter Header range {
    player_id >= 1000;
    player_id <= 2000;
}
```

**当前实现**：
-  未实现

**需要改动**：
- 词法分析器：识别 `..` token
- 解析器：展开为两个条件 `>= min` AND `<= max`

---

#### 2.3 字段默认值

**设计文档要求**：
```pdef
Header {
    uint32 magic = 0x47414D45;  // 默认值
    uint16 type;
}

filter Header auto {
    // magic 自动检查（隐式）
    type = 1;
}
```

**当前实现**：
-  未实现

**需要改动**：
- 解析器：解析 `= value` 默认值
- 编译器：如果 filter 未指定该字段，自动添加检查

---

### 3. 低优先级（锦上添花）

#### 3.1 浮点类型 `float` / `double`

**设计文档要求**：
```pdef
Position {
    float x;
    float y;
    double altitude;
}
```

**当前实现**：
-  未定义 `FIELD_TYPE_FLOAT` / `FIELD_TYPE_DOUBLE`
-  未实现浮点比较指令

**需要改动**：
1. 添加类型定义
2. 添加 `OP_LOAD_FLOAT_BE/LE`, `OP_LOAD_DOUBLE_BE/LE`
3. 浮点比较逻辑（注意精度问题）

**使用场景**：
- 游戏坐标（但通常用定点数）
- 物理引擎数据

---

#### 3.2 端口索引优化

**设计文档要求**：
```cpp
// 启动时构建索引
std::unordered_map<int, std::vector<CompiledFilterRule*>> port_index;

// 运行时快速查找
auto it = port_index.find(dst_port);
if (it != port_index.end()) {
    for (auto* rule : it->second) {
        if (execute(rule, packet, len)) {
            return true;
        }
    }
}
```

**当前实现**：
-  未实现端口索引
-  当前遍历所有协议的所有过滤器（性能可接受）

**需要改动**：
- 在 `protocol.c` 中添加端口索引
- 加载时构建索引
- 运行时使用索引

---

## 📊 完成度总结

| 模块 | 完成度 | 说明 |
|------|--------|------|
| **核心语法** | 100% |  结构定义、嵌套、filter |
| **基础数据类型** | 85% |  缺少 float/double |
| **比较操作符** | 70% |  缺少 in/!in/正则 |
| **嵌套访问** | 90% |  缺少数组索引 |
| **字节码编译** | 100% |  完全实现 |
| **运行时执行** | 100% |  零拷贝、边界检查 |
| **配置验证** | 60% |  缺少变长字段校验 |
| **性能优化** | 90% |  缺少端口索引 |

**总体完成度**: **~85%**

---

## 🎯 推荐的实现优先级

### 阶段1：核心功能补全（高优先级）

1. **变长字段校验** 
   - 工作量：小（1-2小时）
   - 影响：防止配置错误
   - 代码位置：`parser.c`

2. **集合操作符 `in` / `!in`** 
   - 工作量：中（4-6小时）
   - 影响：大幅简化配置
   - 需要改动：lexer, parser, executor

3. **对象数组** 
   - 工作量：大（8-12小时）
   - 影响：支持复杂协议
   - 需要改动：parser（字段展开逻辑）

### 阶段2：增强功能（中优先级）

4. **范围语法糖** 
   - 工作量：小（2-3小时）
   - 影响：提升易用性
   - 可以等到阶段1完成后再做

5. **字段默认值** 
   - 工作量：中（3-4小时）
   - 影响：减少重复配置

### 阶段3：可选功能（低优先级）

6. **正则匹配** 
   - 工作量：大（需引入PCRE库）
   - 影响：支持文本协议
   - 性能影响：较大

7. **浮点类型** 
   - 工作量：中
   - 影响：使用场景少

8. **端口索引** 
   - 工作量：小
   - 影响：性能提升有限（当前已足够快）

---

## 🚀 当前可用性评估

###  可以立即使用的场景

1. **二进制协议** - 完美支持
   - 游戏协议（固定结构）
   - RPC框架（Protobuf/Thrift头部）
   - 数据库协议（MySQL/Memcached）

2. **简单过滤规则**
   - 基于字段值匹配
   - 范围检查（用 >= 和 <= 组合）
   - 位标志检查

###  有限制的场景

1. **多值匹配** - 需要写多个filter
   ```pdef
   // 当前做法（workaround）
   filter Header type1 { type = 1; }
   filter Header type2 { type = 2; }
   filter Header type3 { type = 3; }

   // 期望做法（待实现）
   filter Header types { type in [1, 2, 3]; }
   ```

2. **数组字段** - 只能检查整个数组
   ```pdef
   // 当前做法
   Item {
       uint32 item_id;
       uint16 count;
   }

   // 不能定义：Item items[10]
   // workaround: 手动展开
   Inventory {
       Item item0;
       Item item1;
       Item item2;
       // ...
   }
   ```

###  不支持的场景

1. **文本协议** - 无正则匹配
   - HTTP URI过滤
   - 字符串模式匹配

2. **动态长度字段** - 无法精确处理
   - TLV格式
   - Protobuf变长编码

---

## 💡 使用建议

### 当前最佳实践

1. **优先使用整数类型字段**
   - 性能最优（<100ns）
   - 完全支持

2. **多值匹配的workaround**
   ```pdef
   // 定义多个filter（OR关系）
   filter Packet type1 { type = 1; }
   filter Packet type2 { type = 2; }
   filter Packet type3 { type = 3; }
   ```

3. **范围检查的写法**
   ```pdef
   filter Packet range {
       value >= 1000;
       value <= 2000;
   }
   ```

4. **避免变长字段**
   - 使用固定大小 `byte[256]` 代替 `byte[]`
   - 确保变长字段在最后

---

## 📝 文档同步建议

需要更新的文档：
1. `../README_PDEF.md` - 标注未实现特性
2. `../PDEF_USAGE_GUIDE.md` - 添加限制说明
3. `config/protocols/README.md` - 更新语法说明

---

## 🔧 快速修复清单

如果要快速提升完成度到 **95%**，只需实现：

1.  变长字段校验（1小时）
2.  `in` / `!in` 操作符（6小时）
3.  范围语法糖（2小时）

**总工作量**: 约 1 天

这样就能覆盖 95% 的实际使用场景！

---

## 结论

**当前实现已经非常扎实**，核心功能（85%）已经可以投入生产使用。主要缺失的是：
1. 集合操作符 `in` - 影响配置简洁性
2. 对象数组 - 影响复杂协议支持
3. 变长字段校验 - 影响配置安全性

**建议**: 先补全"阶段1"的3个功能，即可达到 **生产就绪** 标准。
